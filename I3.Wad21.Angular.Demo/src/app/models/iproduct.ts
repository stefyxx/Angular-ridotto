//Interface générée pour simplifier la récupération de donnée de manières ordonnées
// ng generate interface dossier/nomInterface

export interface Iproduct {
    name : string;
    price : number;
    quantity : number;
}
